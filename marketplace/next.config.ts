import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Skills repo data fetched from GitHub API at request time
  // No need for ISR since data is dynamic
};

export default nextConfig;
